::  urui-config: unit tests.
::
::  Phase 4 (W4.1) moves the generic assertions here from the consumer's
::  own web tests.  The file exists now so that `-test` builds the
::  library on every run: a stub that does not compile is not a stub.
::
/+  *urui-config
|%
--
